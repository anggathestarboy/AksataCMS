{{--
    Template Section: Hero Section (hero-section)
    =========================================================
    Variabel: $sectionType, $section, $locale, $content, $fields

    Contoh akses:
        @field('key-name')
    =========================================================
--}}
<section>
        @field('title')
    @field('desc')
    @field('image')
</section>