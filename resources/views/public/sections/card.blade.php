{{--
    Template Section: Card (card)
    =========================================================
    Variabel: $sectionType, $section, $locale, $content, $fields

    Contoh akses:
        @field('key-name')
    =========================================================
--}}
<section>
        <img
        src="@field('card_img')"
        alt="-"
        title="-"
        width="-"
        height="-"
        loading="lazy"
        decoding="async"
    >
</section>