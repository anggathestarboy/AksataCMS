<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Supported locales
    |--------------------------------------------------------------------------
    |
    | Locale codes mapped to display names. The structure of a page
    | (section order & types) is shared across locales, while titles,
    | slugs, meta and section content are translated per locale.
    |
    */

    'locales' => [
        'id' => 'Indonesian',
        'en' => 'English',
    ],

    'default_locale' => 'id',

    /*
    |--------------------------------------------------------------------------
    | Home page slug
    |--------------------------------------------------------------------------
    |
    | Fallback slug (in the default locale) used to resolve the home page at
    | "/" when no home page is explicitly chosen in the global settings.
    |
    */

    'home_slug' => 'beranda',

];
